import 'package:flutter/material.dart';

const navbarBackground = Colors.black26;

const blockStyle = BoxDecoration(
  color: Colors.white10,
  borderRadius: BorderRadius.all(Radius.circular(20)),
);
